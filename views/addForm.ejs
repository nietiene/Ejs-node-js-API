<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add User</title>
      <script src="https://cdn.tailwindcss.com"></script>

</head>
<body class="max-h-screen flex items-center justify-center bg-green-100">
    <form action="/add" method="post" class="bg-green-200 p-8 rounded-lg w-full max-w-md shadow-lg">
        <h1 class="text-2x; font-bold text-center text-green-600 mb-6">Add User</h1>
        <div class="mb-4">
           <label class="block text-green-800 mb-1">Name</label>
           <input type="text" name="name"
            class="w-full px-5 py-2 border border-gray-300 rounded focus:outline-none focus:ring-green-400"><br>
        </div>
        <div class="mb-4">
          <label class="block text-green-800 mb-1">Password</label>
          <input type="password" name="password"
          class="w-full px-5 py-2 border border-gray-300 rounded focus:outline-none focus:ring-green-400"><br>
        </div>
        <div class="mb-6">
            <label class="block text-green-800 mb-1">Role</label>
            <select name="role"
            class="w-full py-2 border border-gray-300 rounded focus:outline-none focus:ring-2 focus:ring-green-400 bg-white text-green-800">
            <option value="user">User</option>
            <option value="admin">Admin</option>
         </select>
        </div>

        <button type="submit" class="w-[50%] bg-green-500 px-4 py-3 text-white shadow-lg hover:bg-green-600 transition duration-300 mb-2">Add</button>
        <button type="submit" class="bg-red-600 w-[50%] py-2 text-white hover:bg-red-500 transition duration-200 shadow-lg rounded ">Cancel</button>
    </form>
</body>
</html>