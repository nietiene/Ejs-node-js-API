<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Welcome <%= user.name %> (<$= sessionUser.role %>)</h1>
    <ul>
        <% user.forEach((user) => { %>
           <li>Id: <%= user.id %></li>
           <li>Name: <%= user.name %></li>
           <li>Password: <%= user.password %></li>
        <% }) %>
    </ul>
</body>
</html>